{
  "name": "express-reaccess",
  "version": "0.0.0",
  "description": "Express middleware to manage API access on a RegExp basis",
  "main": "index.js",
  "scripts": {
    "test": "node_modules/mocha/bin/mocha tests/*.mocha.js",
    "coveralls": "./node_modules/istanbul/lib/cli.js cover ./node_modules/mocha/bin/_mocha --report lcovonly -- tests/*.mocha.js -R spec -t 5000 && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "cover": "./node_modules/istanbul/lib/cli.js cover --report html ./node_modules/mocha/bin/_mocha -- tests/*.mocha.js -R spec -t 5000"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/SimpliField/express-reaccess.git"
  },
  "keywords": [
    "express",
    "auth",
    "acess",
    "regexp"
  ],
  "author": "Nicolas Froidure (Simplifield)",
  "license": "BSD-2-Clause",
  "bugs": {
    "url": "https://github.com/SimpliField/express-reaccess/issues"
  },
  "devDependencies": {
    "mocha": "~1.18.0",
    "supertest": "~0.9.0",
    "express": "~3.4.8",
    "mocha-lcov-reporter": "0.0.1",
    "coveralls": "~2.7.1",
    "istanbul": "~0.2.4"
  }
}
